---
import 'aos/dist/aos.css';
---

<section
  id="nosotros"
  class="container mx-auto px-4 my-20 flex flex-col gap-16 text-white overflow-hidden"
>
  <!-- Encabezado -->
  <div class="text-center" data-aos="fade-down">
    <h1 class="text-3xl font-semibold text-[#902727]">Nosotros</h1>
    <div class="w-12 h-1 bg-[#902727] rounded-full mx-auto mt-2"></div>
  </div>

  <!-- Contenido principal -->
  <div class="flex flex-col md:flex-row items-center gap-12 md:gap-20">
    <!-- Texto -->
    <div class="flex-1 space-y-6" data-aos="fade-right">
      <blockquote class="italic text-[#6C757D] space-y-4">
        <h2 class="text-2xl md:text-3xl font-medium leading-tight">
          "Gestionar una Habilitación es algo que no parece difícil.
          <br class="hidden md:block" />
          <span class="font-bold text-[#902727]">De hecho, muchos lo intentan.</span>
        </h2>
        <p class="text-2xl md:text-3xl font-medium italic">
          Nosotros nos ocupamos, y en eso está la diferencia.
          <br />
          En entender que <span class="font-bold text-[#902727]">los trámites empiezan y deben terminar."</span>
        </p>
      </blockquote>

      <!-- Caja de logro -->
      <div class="relative pl-10 text-[#6C757D] text-base md:text-lg mt-4 leading-relaxed border-l-4 border-[#902727]">
        <i class="fas absolute left-0 top-0 text-2xl text-[#902727] -translate-x-1/2"></i>
        <span class="text-[#902727] font-semibold">Más de 2.000 habilitaciones</span> nos consolidan como líderes del rubro.
        <br />
        Cada trámite es una historia de <span class="italic">confianza</span> y <span class="italic">cumplimiento</span>.
      </div>
    </div>

    <!-- Imagen -->
    <div class="flex flex-col md:flex-row items-center gap-6 md:gap-4" data-aos="fade-left">
      <div class="flex flex-col items-center gap-2 w-full md:w-auto max-w-xs">
        <img 
          src="/about01.jpg" 
          alt="Imagen sobre nosotros 1" 
          class="w-full rounded-lg shadow-lg object-cover"
        />
        <span class="text-[#902727] font-semibold text-center">JORGE LUIS TOLEDO</span>
      </div>
      <div class="flex flex-col items-center gap-2 w-full md:w-auto max-w-xs">
        <img
          src="/about02.jpg" 
          alt="Imagen sobre nosotros 2" 
          class="w-full rounded-lg shadow-lg object-cover"
        />
        <span class="text-[#902727] font-semibold text-center">ROMINA ALEJANDRA MORENO</span>
      </div>
    </div>
  </div>
</section>



<script>
  import AOS from 'aos';
  
  // Inicializar AOS
  AOS.init({
    once: true, // La animación solo ocurre una vez
    offset: 100, // Offset (en px) desde el punto de activación
    duration: 1000, // Valores en ms
    easing: 'ease-in-out',
  });
</script>
